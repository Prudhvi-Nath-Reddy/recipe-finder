---
- name: Copy Frontend Docker Compose file from host machine to remote host
  copy:
    src: ../frontend/docker-compose.yml
    dest: ./

- name: Pull and run Frontend Docker image
  shell:
    cmd: docker-compose up -d --build
    chdir: ./