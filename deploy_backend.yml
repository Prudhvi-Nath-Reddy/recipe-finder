---
- name: Copy Backend Docker Compose file from host machine to remote host
  copy:
    src: ../backend/docker-compose.yml
    dest: ./

- name: Pull and run Backend Docker image
  shell:
    cmd: docker-compose up -d --build
    chdir: ./